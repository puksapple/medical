spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medical?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 4790

brevo:
  api-key:
  sender-email: mynameispukararyal@gmail.com
  sender-name: Medical App